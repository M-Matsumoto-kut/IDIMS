package com.example.idims;

import androidx.appcompat.app.AppCompatActivity;

public class Authenticate extends AppCompatActivity {

    //IDとパスワードが一致しているか検証(1：一致，2:不一致)
    public static boolean loginAuthenticate(String id, String password) {
        boolean match = false;

        //idを元にDBの

        return match;
    }

    //現在のパスワードが一致しているか検証(1：一致，2:不一致)
    public static boolean nowPasswordAuthenticate(String id, String nowPassword) {
        boolean match = false;


        return match;
    }

    //新しいパスワードが規定通りか検証(1：一致，2:不一致)
    public static boolean newPasswordAuthenticate(String id, String newPassword) {
        boolean match = false;


        return match;
    }
}
